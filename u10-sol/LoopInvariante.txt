1.

0 <= i <= arr.length && m = min(arr, i)              // Version A
0 <= i <= arr.length && ∀ 0 <= j < i . m <= arr[j]   // Version B          
                             

2.

str1 != null && str2 != null && s1 != null && s2 != null && (s1 + s2).equals(str1 + str2)
