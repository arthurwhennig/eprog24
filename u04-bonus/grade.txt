NETHZ: ahennig

Total Points: 4.666666667
Total Percentage: 93%

Timeout: NO

testContainsSubstringAt01True: PASS
testContainsSubstringAt02True: PASS
testContainsSubstringAt03True: PASS
testContainsSubstringAt04False: PASS
testContainsSubstringAt05False: PASS
testContainsSubstringAt06False: FAIL
testContainsSubstringAt07False: PASS
testContainsSubstringAt08False: FAIL
testContainsSubstringAt09True: PASS
testContainsSubstringAt10True: PASS
testCountSubstrings01Advanced: PASS
testCountSubstrings02Advanced: PASS
testCountSubstrings03Advanced: PASS
testCountSubstrings04Advanced: PASS
testCountSubstrings05Basic: PASS
testCountSubstrings06Advanced: PASS
testCountSubstrings07Basic: PASS
testCountSubstrings08Basic: PASS
testCountSubstrings09Advanced: PASS
testCountSubstrings10Advanced: PASS
testCountDisjointSubstrings01Advanced: PASS
testCountDisjointSubstrings02Advanced: PASS
testCountDisjointSubstrings03Advanced: PASS
testCountDisjointSubstrings04Advanced: PASS
testCountDisjointSubstrings05Basic: PASS
testCountDisjointSubstrings06Advanced: PASS
testCountDisjointSubstrings07Basic: PASS
testCountDisjointSubstrings08Basic: PASS
testCountDisjointSubstrings09Advanced: PASS
testCountDisjointSubstrings10Advanced: PASS


Folgende Test-Datei wurde für die Bewertung verwendet:

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class GradingCountingTest {

    /**
     * Given <code>sub</code> is prefix of <code>str</code> then return <code>true</code>
     */
    @Test
    public void testContainsSubstringAt01True() {
        assertTrue(Counting.containsSubstringAt("sdflkj", 0, "sdf"));
    }

    /**
     * Given <code>sub</code> is suffix of <code>str</code> then return <code>true</code>
     */
    @Test
    public void testContainsSubstringAt02True() {
        assertTrue(Counting.containsSubstringAt("poijwe", 3, "jwe"));
    }

    /**
     * Given <code>sub</code> is proper substring of <code>str</code> then return <code>true</code>
     */
    @Test
    public void testContainsSubstringAt03True() {
        assertTrue(Counting.containsSubstringAt("wdlfkjersdf!!!43253sdf43jkl36894", 9, "df!!!4"));
        assertTrue(Counting.containsSubstringAt("abcabcabc", 3, "abc"));
    }

    /**
     * Given <code>sub</code> is not substring of <code>str</code> then return <code>false</code>
     */
    @Test
    public void testContainsSubstringAt04False() {
        assertFalse(Counting.containsSubstringAt("abcdefghi", 1, "bca"));
        assertFalse(Counting.containsSubstringAt("abcdefghi", 3, "cdef"));
        assertFalse(Counting.containsSubstringAt("abcdefghi", 1, "abc"));
    }

    /**
     * Given <code>position</code> is negative then return <code>false</code>
     */
    @Test
    public void testContainsSubstringAt05False() {
        assertFalse(Counting.containsSubstringAt("abcdefgh", -109, "abc"));
    }

    /**
     * Given <code>sub</code> is larger than <code>str</code> then return <code>false</code>
     */
    @Test
    public void testContainsSubstringAt06False() {
        assertFalse(Counting.containsSubstringAt("2354lksf!!!!??", 0, "2354lksf!!!!??!"));
    }

    /**
     * Given <code>position > str.length()</code> then return <code>false</code>
     */
    @Test
    public void testContainsSubstringAt07False() {
        assertFalse(Counting.containsSubstringAt("abcd", 10, "abcd"));
    }

    /**
     * Given <code>position + sub.length() > str.length()</code> then return <code>false</code>
     */
    @Test
    public void testContainsSubstringAt08False() {
        assertFalse(Counting.containsSubstringAt("abcdefghi", 6, "ghi1"));
        assertFalse(Counting.containsSubstringAt("abcdefghi", 6, "ghi1234"));
    }

    /**
     * Given <code>sub.length() == 200</code> and <code>sub</code> is proper substring of <code>str</code> then return
     * <code>true</code>
     */
    @Test
    public void testContainsSubstringAt09True() {
        final String prefixAndSuffix = "abcd";
        final String middlePart = "t".repeat(200);
        assertTrue(Counting.containsSubstringAt(prefixAndSuffix + middlePart + prefixAndSuffix, 4, middlePart));
    }

    /**
     * Given <code>str</code> is equal to <code>sub</code> and <code>position == 0</code> then return <code>true</code>
     */
    @Test
    public void testContainsSubstringAt10True() {
        assertTrue(Counting.containsSubstringAt("abc", 0, "abc"));
    }

    /**
     * Given <code>sub</code> occurs once in <code>str</code> then return <code>1</code>
     */
    @Test
    public void testCountSubstrings01Advanced() {
        assertEquals(1, Counting.countSubstrings("1234e5", "234e"));
    }

    /**
     * Given <code>sub</code> occurs more than once (non-overlapping) in <code>str</code> then return the number of occurrences
     */
    @Test
    public void testCountSubstrings02Advanced() {
        assertEquals(3, Counting.countSubstrings("12345645ee45tt1", "45"));
        assertEquals(2, Counting.countSubstrings("ttzzttzz", "zz"));
        assertEquals(2, Counting.countSubstrings("ttzzttzz", "tt"));
        assertEquals(3, Counting.countSubstrings("babababb", "ab"));
    }

    /**
     * Given <code>sub</code> occurs more than once (overlapping) in <code>str</code> then return the number of occurrences
     */
    @Test
    public void testCountSubstrings03Advanced() {
        assertEquals(2, Counting.countSubstrings("5432543215432154321123", "5432154"));
        assertEquals(4, Counting.countSubstrings("aababababa", "aba"));
        assertEquals(3, Counting.countSubstrings("cccc", "cc"));
        assertEquals(5, Counting.countSubstrings("ababaccababatabaab", "aba"));
    }

    /**
     * Given <code>sub</code> occurs before the algorithm realizes that the current string is not equal to <code>sub</code>
     * then return the correct number of occurrences
     */
    @Test
    public void testCountSubstrings04Advanced() {
        assertEquals(1, Counting.countSubstrings("aaaaa12", "aaa12"));
        assertEquals(1, Counting.countSubstrings("aaaa12", "aaa12"));
        assertEquals(3, Counting.countSubstrings("aaabaabaabaa", "aabaa"));
    }

    /**
     * Given <code>sub</code> does not occur in <code>str</code> then return <code>0</code>
     */
    @Test
    public void testCountSubstrings05Basic() {
        assertEquals(0, Counting.countSubstrings("abc", "ef"));
    }

    /**
     * Given <code>sub</code> occurs in <code>str</code> 200 times then return <code>200</code>
     */
    @Test
    public void testCountSubstrings06Advanced() {
        assertEquals(200, Counting.countSubstrings("abc".repeat(200), "abc"));
        assertEquals(200, Counting.countSubstrings("02".repeat(200) + "0", "020"));

    }

    /**
     * Given <code>sub.length() > str.length()</code> then return <code>0</code>
     */
    @Test
    public void testCountSubstrings07Basic() {
        assertEquals(0, Counting.countSubstrings("abcdefg", "abcdefgh"));
        assertEquals(0, Counting.countSubstrings("abcdefg", "abcdefghijkl"));
    }

    /**
     * Given <code>sub.length() + position > str.length()</code> where <code>position</code> stands for the index where
     * <code>sub</code> occurs for the first time in <code>str</code> then return <code>0</code>
     */
    @Test
    public void testCountSubstrings08Basic() {
        assertEquals(0, Counting.countSubstrings("abcdefghi", "ghij"));
    }

    /**
     * Given <code>sub.equals(str)</code> then return <code>1</code>
     */
    @Test
    public void testCountSubstrings09Advanced() {
        assertEquals(1, Counting.countSubstrings("abcd11", "abcd11"));
    }

    /**
     * Given <code>sub</code> occurs once as prefix or suffix in <code>str</code> then return 1
     */
    @Test
    public void testCountSubstrings10Advanced() {
        assertEquals(1, Counting.countSubstrings("epprr00gg", "00gg"));
        assertEquals(1, Counting.countSubstrings("eeeepprooGG", "eeeepp"));
    }

    /**
     * Given <code>sub</code> occurs once in <code>str</code> then return 1
     */
    @Test
    public void testCountDisjointSubstrings01Advanced() {
        assertEquals(1, Counting.countDisjointSubstrings("abcdefg", "bcdef"));
    }

    /**
     * Given <code>sub</code> occurs once as prefix or suffix in <code>str</code> then return 1
     */
    @Test
    public void testCountDisjointSubstrings02Advanced() {
        assertEquals(1, Counting.countDisjointSubstrings("epprr00gg", "00gg"));
        assertEquals(1, Counting.countDisjointSubstrings("eeeepprooGG", "eeeepp"));
    }

    /**
     * Given <code>sub</code> occurs more than once (non-overlapping) in <code>str</code> then return the number of occurrences
     */
    @Test
    public void testCountDisjointSubstrings03Advanced() {
        assertEquals(4, Counting.countDisjointSubstrings("12345645ee45tt1121245!!", "45"));
        assertEquals(3, Counting.countDisjointSubstrings("aabbaabbaabb", "bb"));
        assertEquals(3, Counting.countDisjointSubstrings("aabbaabbaabb", "aa"));
        assertEquals(3, Counting.countDisjointSubstrings("babababb", "ab"));
    }

    /**
     * Given <code>sub</code> occurs more than once (overlapping) in <code>str</code> then return the number of occurrences
     */
    @Test
    public void testCountDisjointSubstrings04Advanced() {
        assertEquals(3, Counting.countDisjointSubstrings("02020202020", "020"));
        assertEquals(2, Counting.countDisjointSubstrings("aababababa", "aba"));
        assertEquals(2, Counting.countDisjointSubstrings("ccccc", "cc"));
    }

    /**
     * Given <code>sub</code> does not occur in <code>str</code> then return <code>0</code>
     */
    @Test
    public void testCountDisjointSubstrings05Basic() {
        assertEquals(0, Counting.countDisjointSubstrings("abc", "ef"));
    }

    /**
     * Given <code>sub</code> occurs in <code>str</code> 200 times (non-overlapping) then return <code>200</code>
     */
    @Test
    public void testCountDisjointSubstrings06Advanced() {
        assertEquals(200, Counting.countDisjointSubstrings("abc".repeat(200), "abc"));
        assertEquals(200, Counting.countDisjointSubstrings("01".repeat(400) + "0", "010"));
    }

    /**
     * Given <code>sub.length() > str.length()</code> then return <code>0</code>
     */
    @Test
    public void testCountDisjointSubstrings07Basic() {
        assertEquals(0, Counting.countDisjointSubstrings("sdfabcdefg", "sdfabcdefgh"));
        assertEquals(0, Counting.countDisjointSubstrings("ttabcdefgt", "ttabcdefghijklt"));
    }

    /**
     * Given <code>sub.length() + position > str.length()</code> where <code>position</code> stands for the index where
     * <code>sub</code> occurs for the first time in <code>str</code> then return <code>0</code>
     */
    @Test
    public void testCountDisjointSubstrings08Basic() {
        assertEquals(0, Counting.countDisjointSubstrings("abcdefghi", "defghik"));
        assertEquals(0, Counting.countDisjointSubstrings("abcdefghikk", "defghiklll"));
    }

    /**
     * Given <code>sub</code> occurs before the algorithm realizes that the current string is not equal to <code>sub</code>
     * then return the correct number of occurrences
     */
    @Test
    public void testCountDisjointSubstrings09Advanced() {
        assertEquals(2, Counting.countDisjointSubstrings("aaaaabaabbaaaab", "aaaab"));
    }

    /**
     * Given <code>sub.equals(str)</code> then return <code>1</code>
     */
    @Test
    public void testCountDisjointSubstrings10Advanced() {
        assertEquals(1, Counting.countDisjointSubstrings("xxabcd1133", "xxabcd1133"));
    }
}